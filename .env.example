# .env.example — variáveis de ambiente para Vite
# Copie este arquivo para ".env" (desenvolvimento) ou ".env.production" (produção)
# Apenas variáveis com prefixo VITE_ ficam disponíveis no frontend via import.meta.env

# Base da API (desenvolvimento/local)
VITE_API_BASE_URL=http://localhost:8000

# Exemplos
# Desenvolvimento:
# VITE_API_BASE_URL=http://localhost:8000
# Produção:
# VITE_API_BASE_URL=https://api.seudominio.com

# Observações
# - Não coloque tokens ou segredos aqui; o app usa localStorage 'auth_token' após login.
# - Se VITE_API_BASE_URL não estiver definido, o frontend usa fallback:
#   https://entrevista-front-end-xm3k.onrender.com